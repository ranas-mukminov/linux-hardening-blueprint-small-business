# Integration Test Notes

Use `tests/integration/molecule/default` to validate roles against container images. The sample inventory at `ansible/inventories/example_inventory.yml` demonstrates the structure expected by the Molecule scenario.
